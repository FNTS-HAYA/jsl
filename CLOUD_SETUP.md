# 自動化のセットアップ

単語を追加したら、保存・全ユーザーへの反映・AIの学習まで自動で走るようにした。
**この手順は最初の1回だけ。** 終われば以降はスタジオで録って押すだけになる。

## スタジオはサイトから分離した

スタジオは `admin/studio.html` に移し、サイトからのリンクを全部外した。
一般のユーザーが辿り着くことはない。自分は URL を直接開く。

```
https://fnts-haya.github.io/jsl/admin/studio.html
```

管理者以外が開いても「この画面は管理用です」と出るだけで、中身は表示されない。

## 何が起きるようになるか

**押すボタンは「1テイク録る」だけ。** 3テイク目を録り終えた瞬間から、以下が自動で走る。

```
3テイク目を録り終える
   ↓ 自動
モデルに組み込まれる（プロトタイプを計算）
   ↓ 自動
Firestore に保存される
     shared/prototypes … 組み込んだモデル
     shared/catalog    … 単語一覧
     contributions     … 生データ（学習の材料）
   ↓ 自動
サイトを開いたユーザーの学習モードと図鑑にその単語が出て、認識できる
   ↓ 最後の収録から1分後に自動
GitHub Actions が学習を走らせる（3〜10分）
   ↓ 終わると
新しいモデルが自動コミットされ、GitHub Pages が更新される
プロトタイプも作り直されて Firestore に書き戻される
```

4テイク目以降を録れば、そのたびに組み込みし直される。精度が上がるだけで手順は増えない。

学習だけ1分待つのは、テイクを録るたびに GitHub Actions が走ると無駄だから。
連続で単語を追加してもまとめて1回になる。すぐ回したいときは「いま学習を回す」を押す。

---

## 手順1: Firestore のルールを入れる

Firebase コンソール → **Firestore Database** → **ルール** → `firestore.rules` の中身を貼って **公開**。

これで期限切れも直る。ルールの方針は「単語一覧とプロトタイプは誰でも読める、書けるのは管理者だけ、生データは誰からも読めない」。

## 手順2: 自分を管理者に登録する

1. `learn.html` を開いて、いつも使うアカウントでログイン（スタジオはこのログインを共有する）
2. Firebase コンソール → **Authentication** → ユーザー一覧から自分の **ユーザーUID** をコピー
3. Firebase コンソール → **Firestore Database** → **コレクションを開始**
   - コレクションID: `admins`
   - ドキュメントID: **コピーしたUID**
   - フィールドは何でもいい（例: `name` に自分の名前）
4. 保存

これで自分だけが単語を追加できるようになる。他の人はアプリを使えるが追加はできない。

## 手順3: サービスアカウントを作る（GitHub Actions が Firestore を読むため）

1. Firebase コンソール → 歯車 → **プロジェクトの設定** → **サービス アカウント**
2. **新しい秘密鍵の生成** → JSONファイルがダウンロードされる
3. GitHub のリポジトリ → **Settings** → **Secrets and variables** → **Actions** → **New repository secret**
   - Name: `FIREBASE_SERVICE_ACCOUNT`
   - Secret: **JSONファイルの中身を全部貼る**
4. ダウンロードしたJSONファイルは消す。**絶対にリポジトリに入れないこと。**

## 手順4: GitHubトークンをスタジオに設定する

スタジオから学習を起動するために要る。

1. GitHub → 右上のアイコン → **Settings** → **Developer settings**
   → **Personal access tokens** → **Fine-grained tokens** → **Generate new token**
2. 設定
   - **Repository access**: Only select repositories → `jsl` だけ選ぶ
   - **Permissions** → Repository permissions → **Contents** を **Read and write**
   - 有効期限は短め（90日など）。切れたら作り直す
3. できたトークンをコピー
4. `admin/studio.html` を開いて右上の **設定**
   - リポジトリ: `FNTS-HAYA/jsl`
   - GitHubトークン: 貼り付け
   - 保存

**トークンはこの端末のブラウザにだけ保存される。** リポジトリにも Firestore にも入らない。
他人のPCで作業するときは設定しない、または使い終わったらトークンを失効させる。

## 手順5: 最初の1回だけ学習を回す

`encoder.onnx` がまだ無いので、プロトタイプ登録が使えない。スタジオの「いま学習を回す」を押すか、手元で:

```
python train.py
```

これで `encoder.onnx` と `prototypes.json` ができる。以降は全自動。

---

## ふだんの使い方

1. `admin/studio.html` を開く
2. 「単語を追加」
3. 「1テイク録る」を3〜8回（毎回少しずつ速さや位置を変える）

以上。3テイク目で勝手に組み込まれ、Firestore に保存され、サイトに出る。
`words.js` を書き換えて push する作業はもう要らない。

画面の下の帯が状態を示す。

| 表示 | 意味 |
|---|---|
| あとNテイクで自動的に組み込まれる | まだ足りない |
| モデルに組み込んでいます… | 処理中 |
| 「単語」を組み込んだ。サイトを開けばもう使える | 完了。全ユーザーに反映済み |
| 組み込みに失敗 | 右のメッセージを見る |

---

## 知っておくべきこと

### エンコーダが変わるとプロトタイプは作り直される

プロトタイプは「特定のエンコーダの空間での座標」なので、再学習してエンコーダが変わると意味を失う。
そのままにすると再学習のたびに認識が謎に壊れるので、**GitHub Actions が学習後に生データからプロトタイプを作り直して Firestore に書き戻す**ようにしてある。
`encoderVersion` で新旧を見分けていて、合わないプロトタイプはブラウザ側が自動で無視する。

### Firebase の無料枠

Spark プラン(無料)は 1 GiB ストレージ、読み取り5万回/日、書き込み2万回/日、下り10 GiB/月。

- 1テイクが約43KB。**1 GiBで2万テイクくらい**入る。単語100個×10テイクで1000テイクなので当分足りる
- 読み取りは、単語一覧とプロトタイプを**それぞれ1ドキュメントにまとめてある**ので1ページあたり2回だけ。1日2万5千ページ表示まで無料枠に収まる
- Spark プランは請求が発生しない。枠を使い切ると翌日まで止まるだけで、カード登録も不要

### プロトタイプのドキュメントサイズ

`shared/prototypes` は1ドキュメントに全単語ぶん入る。Firestore の上限は1ドキュメント1MB。
1単語あたり約1.5KBなので、**400単語くらいで上限に近づく**。そこまで増えたら単語をいくつかのドキュメントに分ける必要がある。

### 生データの索引

`contributions` の `data` は base64 の文字列で持たせている。数値の配列だと Firestore が索引を張ってしまい容量と書き込みコストが増えるため。
気になるならコンソールの「インデックス」→「単一フィールド」で `contributions.data` を除外設定にしておくとより確実。

### トークンが切れたら

スタジオの「AIに登録する」で「学習を起動できませんでした」と出たら、たいていトークンの期限切れ。
作り直して設定に入れ直す。登録そのものは成功しているので、単語は既に全員に反映されている。学習だけあとで回せばいい。

### 動かないときの見どころ

| 症状 | 見るところ |
|---|---|
| 「この画面は管理用です」と出る | 手順2の `admins/{UID}` が正しいか。UIDのコピーミスが多い。learn.html でログイン済みかも確認 |
| 「クラウドに繋がっていません」 | `firebase-config.js` があるか。コンソールにエラーが出ていないか |
| 学習が始まらない | 設定のリポジトリ名とトークン。Actionsタブに実行履歴が出ているか |
| Actionsが失敗する | `FIREBASE_SERVICE_ACCOUNT` の JSON が途中で切れていないか |
| 単語が他の端末で出ない | Firestore の `shared/catalog` を開いて中身があるか |


### 新しいモデルが反映されない

ブラウザが古い `.onnx` をキャッシュしていることがある。
対策として、モデルのURLに `?v=エンコーダの版` を付けて、学習で中身が変わると自動で読み直すようにしてある。
それでも古いままなら Ctrl+Shift+R で強制再読み込み。

### スタジオを完全に隠したいなら

`admin/studio.html` は URL を知っていれば誰でも開けるが、Firestore のルールで書き込みは管理者しかできないので、
他人が開いても何も保存できない。それでも存在自体を隠したいなら、スタジオだけ別リポジトリにして
ローカルでだけ動かすか、GitHub Pages に含めず手元で `python -m http.server` して使う手もある。
その場合も Firestore への保存はそのまま動く。
