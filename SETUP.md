# HANDIT — 今回の変更まとめ

## 1. まずやること：Firestore の期限切れを直す

テストモードのルールは30日で切れる。それが原因。

1. Firebase コンソール → **Firestore Database** → **ルール** タブ
2. 中身を全部消して `firestore.rules` の中身を貼る
3. **公開** を押す

これで期限切れは二度と起きない。中身は「本人だけが自分の進捗を読み書きできる」というルールになっている。
`contributions` は、あとで他の人から手話データを集めるとき用に書き込みだけ開けてある。

---

## 2. 単語の追加が1箇所で済むようになった

これまで単語は `learn.html` の `LEVELS`、`learn.html` の `DICTIONARY`、`dictionary.html` の `DICTIONARY` の
3箇所に書く必要があった。これを **`words.js` の1箇所** にまとめた。

```js
// words.js
const WORDS = [
  { word: 'おつかれさま', cat: 'あいさつ', level: 1, gif: 'assets/otsukare.gif', desc: '右手を額から前に下ろす' },
];
```

この1行を足すだけで、学習マップ・レッスンのお手本・手話図鑑にすべて反映される。
`LEVEL_META` に新しいレベル番号を足せばレベルも増やせる。

**注意**: `word` は AI のラベル名と1文字も違わないようにすること。ここがずれると永久にクリアできないレベルができる。

---

## 3. 新しい手話を覚えさせる流れ

### いつもの流れ

1. `admin/studio.html` を開く
2. 「単語を追加」→ 単語名・カテゴリ・説明を入れる
3. 「1テイク録る」を **3〜8回**。速さや立ち位置を少しずつ変えると強くなる
4. **「AIに登録する」** を押す → その場で認識できるようになる（レッスンでも自由手話モードでも使える）
5. あとでまとめて「データを書き出す」→ ダウンロードした JSON を学習にかける

```bash
pip install torch numpy onnx
python train.py --add ~/Downloads/handit_dataset_2026-09-04.json
```

6. できた `dataset/model_single.onnx` `dataset/encoder.onnx` `dataset/labels.json` を push
7. `words.js` に単語を1行足す

### 「過去のデータが消えない」しくみ

- 取り込んだテイクは `dataset/words/*.npz` に単語ごとに保存され、**消えない**
- `train.py` は毎回そこにある全データを使うので、**再収録は要らない**
- 前回の `checkpoint.pt` から重みを引き継ぎ（ウォームスタート）、分類ヘッドだけ行を増やす
  → 前からある単語の分類ヘッドはそのまま再利用されるので、精度が急に落ちない
- 同じファイルを2回 `--add` しても、テイクのハッシュで重複判定するので二重に増えない

### 便利なコマンド

```bash
python train.py --list      # いま何単語 / 何テイク持っているか
python train.py             # 取り込み済みのデータだけで学習し直す
python train.py --fresh     # ウォームスタートせず最初から
```

### 複数人からデータを集めるとき

集めたい人にも `admin/studio.html` を触ってもらい、「データを書き出す」でファイルをもらう。
自分の環境で `python train.py --add 相手のファイル.json` すれば合流する。
`admin/studio.html` の「データを読み込む」でも、既存データに**追加**する形で取り込める。

---

## 4. 右手・左手どちらでもよくなった

手話は利き手がどちらでも同じ意味になるので、学習時に**データを左右反転したものを自動生成**して一緒に覚えさせている。
`train.py` が学習データを2倍にするので、**片手ぶんだけ録れば両方に対応する**。

反転の中身は「右手と左手のスロットを入れ替え、x座標を反転し、手の向きベクトルと手のひらの法線の符号を直し、
顔の左目と右目も入れ替える」というもの。`js/features.js` の `mirrorVec` と `train.py` の `mirror` が
同じ計算をしている（両者が完全一致することは確認済み）。

**特徴量を変えるときは必ず両方を同時に直すこと。** 片方だけ直すと、学習と推論がずれて精度が静かに壊れる。

---

## 5. ホーム画面

`index.html` を作り直した。

- 先頭にあった `// trigger redeploy` を削除（`<!DOCTYPE>` より前に文字があるとブラウザが互換モードで解釈する）
- 「64フレームぶんの動きを見て判定している」というこのアプリ固有の見た目を軸にした
- 「しくみ」のセクションで、カメラ → 21点+顔6点 → 168次元×64フレーム → Transformer → 単語 という
  実際の処理を説明している。研究として見せるならここが効く
- 「AIが判定できる単語」の数を `labels.json` と即席登録から実際に数えて出している
- 全部の要素がフワッと出てくるアニメーションはやめて、読み込み時の1回だけにした
- キーボードのフォーカス表示、`prefers-reduced-motion` への対応を入れた

---

## ファイルの置き場所

```
jsl/
├── index.html              作り直したホーム
├── learn.html              学習（words.js と共通モジュールを使うよう修正済み）
├── free.html               自由手話モード（type="module" に変更）
├── dictionary.html         手話図鑑（words.js から読むよう修正済み）
├── admin/studio.html       ★新規 単語スタジオ（サイトから分離）
├── tutorial.html           初回チュートリアル（変更なし）
├── words.js                ★新規 単語の唯一の置き場所
├── app.js                  自由手話モードの画面処理（共通モジュール利用に書き換え）
├── style.css               変更なし
├── train.py                ★新規 学習スクリプト
├── firestore.rules         ★新規 Firestore のルール
├── js/
│   ├── features.js         ★新規 特徴量の計算とミラー変換
│   └── recognizer.js       ★新規 ONNX 推論 + プロトタイプ照合
├── assets/
│   └── konnichiwa.gif      お手本（ここに置く）
└── dataset/
    ├── model_single.onnx   分類モデル
    ├── encoder.onnx        ★新規 埋め込みエンコーダ（即席登録に必要）
    ├── labels.json         単語の並び順
    ├── checkpoint.pt       ウォームスタート用
    └── words/*.npz         収録したデータ（消さないこと）
```

---

## 動かす前に確認すること

- **`encoder.onnx` は最初は存在しない。** `python train.py` を一度回すと出てくる。
  それまでスタジオの「AIに登録する」は使えない（収録と書き出しはできる）。
- ES モジュールを使うので、**`file://` で直接開くと動かない**。
  `python3 -m http.server 8000` などローカルサーバー経由で開く。
- `dataset/words/` は容量が増える。Git LFS を使うか、`.gitignore` に入れて手元とバックアップで持つか決めておく。
  今は普通に追跡される設定にしてある。

## 気づいた小さいバグ（直しておいた）

- `index.html` の `// trigger redeploy` 削除
- レッスン中に画面幅が変わるとカメラが消える問題（開始時のカメラ枠を覚えておくようにした）
- 特徴量の関数が4ファイルに丸ごとコピーされていたのを `js/features.js` に集約

## 直していない、判断が要るもの

`learn.html` の `onAuthStateChanged` は、**ログアウトすると `handit_xp` と `handit_cleared` を問答無用で 0 に戻す**。
ログイン前にゲストとして進めた分も消える。意図通りならそのままでいい。
残したいなら、ログアウト時にリセットせず、ログイン時に Firestore の値と大きい方を採用する形にする。
